# Running Drupal Using docker-compose

## Usage

Make sure you have Docker installed on your machine.

To start the stack:

```bash
$ ./script.sh up
```

After the stack starts up, you are now able to visit http://localhost:8080 in your browser.

To tear down the stack:

```bash
$ ./script.sh down
```

